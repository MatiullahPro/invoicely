export const metadata = {
    title: "Billing History & Ledger Registry | Invoicely",
    description: "Review and manage your saved invoices and receipts. Filter by client, date, type, or amount. All stored 100% private and locally.",
    keywords: ["billing history", "invoice records", "saved receipts", "accounting ledger", "offline billing records"],
};

export default function HistoryLayout({ children }) {
    return children;
}
