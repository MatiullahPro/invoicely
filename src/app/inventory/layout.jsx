export const metadata = {
    title: "Product & Service Inventory Manager | Invoicely",
    description: "Track and manage products, hourly service rates, and standard pricing for quick offline invoicing. No backend server required.",
    keywords: ["inventory tracker", "service catalog", "product list", "billing items", "offline stock inventory"],
};

export default function InventoryLayout({ children }) {
    return children;
}
