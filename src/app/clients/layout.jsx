export const metadata = {
    title: "Customer Directory & Client Manager | Invoicely",
    description: "Manage your client database locally and securely. Add contact info, billing addresses, and generate invoices in a single click.",
    keywords: ["client directory", "customer database", "client manager", "freelance customer list", "offline CRM"],
};

export default function ClientsLayout({ children }) {
    return children;
}
